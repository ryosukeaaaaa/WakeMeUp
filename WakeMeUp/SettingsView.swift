import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("設定")
            .navigationTitle("設定")
    }
}

struct SettingsView_Previews: PreviewProvider {
    static var previews: some View {
        SettingsView()
    }
}
